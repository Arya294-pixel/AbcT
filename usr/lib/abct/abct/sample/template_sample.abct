template <typename T>

class container {
	private:
    	data: T;

    public:
    
    # Standard C++ style constructor
    	fn container(input: T) -> NoReturnType{
        	data = input;
    	}
}

fn main() -> int{
	MyData:container<str> = container("data");
	return 0;
}
